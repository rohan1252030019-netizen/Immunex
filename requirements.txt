# ── Core scientific stack ──────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
faiss-cpu>=1.7.4
joblib>=1.3.0

# ── Async / API framework ─────────────────────────────────────────────────────
uvicorn[standard]>=0.23.0
fastapi>=0.103.0
httpx>=0.25.0
aiofiles>=23.0.0

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler>=3.10.0

# ── Graph & correlation ───────────────────────────────────────────────────────
networkx>=3.1

# ── Data validation & storage ─────────────────────────────────────────────────
pydantic>=2.0.0
sqlmodel>=0.0.14
sqlite-utils>=3.35

# ── Logging ───────────────────────────────────────────────────────────────────
loguru>=0.7.0

# ── Layer 3: LangChain + LangGraph orchestration ──────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langgraph>=0.1.0

# ── Layer 3: Local LLM (Ollama) ───────────────────────────────────────────────
ollama>=0.2.0

# ── Layer 3: Embeddings / transformers (CPU-only) ────────────────────────────
transformers>=4.40.0
sentence-transformers>=3.0.0
# Install torch CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# ── System observability ──────────────────────────────────────────────────────
psutil>=5.9.0

# ── Terminal UI ───────────────────────────────────────────────────────────────
rich>=13.0.0

# ── Optional: async MQTT for SIEM ingestion ───────────────────────────────────
asyncio-mqtt>=0.16.0

# ── Optional: Redis cache (for distributed deployments) ───────────────────────
# redis>=5.0.0

# ── Layer 5: Enterprise SOC Operations & Zero-Trust ───────────────────────────
pyjwt>=2.8.0
reportlab>=4.0.0
